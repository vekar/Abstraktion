%title= "{{ page.title }}"

%meta{ name: "viewport", content: "width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0" }

%link{ href: "{{ 'favicon.png' | asset_path }}", rel: "shortcut icon", type: "image/png" }/

{{ "app" | stylesheet }}
